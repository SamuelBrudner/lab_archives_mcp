name: labarchives-mcp-pol
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - openssl
  - libxml2
  - libxslt
  - setuptools>=68
  - wheel
  - pip:
      - httpx>=0.27
      - pydantic>=2.7
      - loguru>=0.7
      - hydra-core>=1.3
      - tenacity>=8.3
      - omegaconf>=2.3
      - pytest>=8.3
      - pytest-asyncio>=0.23
      - mypy>=1.9
      - ruff>=0.5
      - black>=24.4
      - isort>=5.13
      - pre-commit>=3.7
