name: labarchives-mcp-pol
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - openssl
  - libxml2
  - libxslt
  - setuptools>=68
  - wheel
  - pip:
      - httpx>=0.27
      - lxml>=5.2
      - pydantic>=2.7
      - loguru>=0.7
      - hydra-core>=1.3
      - tenacity>=8.3
      - omegaconf>=2.3
      - fastmcp>=2.1
      - aiofiles>=24.1
      - notebook>=7.2
      # Vector backend dependencies
      - tiktoken>=0.7
      - langchain>=0.2
      - openai>=1.40
      - pandas>=2.2
      - pyarrow>=16.0
      - pinecone>=4.1
      - markdown>=3.5
      - Pygments>=2.15
      - qdrant-client>=1.9
      - dvc>=3.0
      - filelock>=3.13
      # Dev/testing dependencies
      - pytest>=8.3
      - pytest-asyncio>=0.23
      - pytest-cov>=5.0
      - pytest-benchmark>=4.0
      - hypothesis>=6.100
      - vcrpy>=6.0
      - respx>=0.21
      - mutmut>=2.4
      - mypy>=1.9
      - types-PyYAML>=6.0
      - types-aiofiles>=23.0
      - types-Markdown>=0.1
      - ruff>=0.5
      - black>=24.4
      - isort>=5.13
      - pre-commit>=3.7
      - nbstripout>=0.6
      - nbdime>=4.0
      - commitizen>=3.12
