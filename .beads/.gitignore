# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Local sync/runtime metadata
.local_version
last-touched
metadata.json
sync-state.json
beads.left.meta.json
daemon.lock

# Keep JSONL exports (source of truth for git)
!*.jsonl

# Except daemon spill files
beads.left.jsonl
